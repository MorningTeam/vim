git submodule init
git submodule update --recursive
git rm --cached bundle/

go get -u github.com/jstemmer/gotags
go get -u github.com/rogpeppe/godef


gd      go to definition
Ctrl_o  go back

Ctags
    Ctrl_], Ctrl_t navigation between definition
    Ctrl_w Ctrl_]   split a new window
    :tjump     taglist

Install java8
http://www.webupd8.org/2014/03/how-to-install-oracle-java-8-in-debian.html

Install Genymotion
http://www.sysads.co.uk/2014/06/install-genymotion-in-ubuntu-14-04/

Install YouCompleteMe
    sudo apt-get install build-essential cmake
    ./install.sh --clang-completer --gocode-completer

Install airline
    `git clone https://github.com/powerline/fonts.git`  or
    `wget https://github.com/powerline/fonts/archive/master.zip`

    run ./install.sh
    Edit -> Profile Preferences -> custom font -> Source Code Pro for Powerline Medium 13(font-size)

## tmux
scroll:
    Ctrl_b [

Ctrl-b then [ then you can use your normal navigation keys to scroll around (eg. Up Arrow or PgDn). Press q to quit scroll mode.

Alternatively you can press Ctrl-b PgUp to go directly into copy mode and scroll one page up (which is what it sounds like you will want most of the time)

You can also scroll up/down line by line using Shift-k and Shift-j (if you're already in scroll mode)

### tmux plugin manager
run '~/.tmux/tpm/tpm'
tmux source ~/.tmux.conf

**Key Bindings**
prefix + I
    * installs new plugins from github or any other git repo
    * refreshes TMUX environment
prefix + U
    * updates plugin(s)
prefix + alt + u
    * remove/uninstall plugins not on the plugin list
## Silver Search
apt-get install silversearcher-ag

## Change Font Size
zoom in Ctrl_Shift_+
zoom out Ctrl_-
